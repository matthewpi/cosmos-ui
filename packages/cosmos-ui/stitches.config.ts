import { createStitches } from '@stitches/react';

export const { css, config, globalCss, keyframes, styled, theme, getCssText } = createStitches();
export type { CSS } from '@stitches/react/types/css-util';
