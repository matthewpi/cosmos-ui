import { StateSlice } from '~/store';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface FlashSlice {}

export const createFlashSlice: StateSlice<FlashSlice> = () => ({});
